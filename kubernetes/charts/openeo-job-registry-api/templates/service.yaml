apiVersion: v1
kind: Service
metadata:
  name: {{ include "openeo-job-registry-api.fullname" . }}
  labels:
    {{- include "openeo-job-registry-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "openeo-job-registry-api.selectorLabels" . | nindent 4 }}

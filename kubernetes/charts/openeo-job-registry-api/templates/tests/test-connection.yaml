apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "openeo-job-registry-api.fullname" . }}-test-connection"
  labels:
    {{- include "openeo-job-registry-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "openeo-job-registry-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "openeo-workspaces-api.serviceAccountName" . }}
  labels:
    {{- include "openeo-workspaces-api.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}


#!/usr/bin/env groovy

@Library('lib')_

helmchartPipeline{
  chart_path       = './kubernetes/charts/sparkapplication'
  chart_repository = 'helm-public'
  chart_version    = '1.6.0'  // Should from now on be aligned with CHANGELOG.md
}

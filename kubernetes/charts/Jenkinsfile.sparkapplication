#!/usr/bin/env groovy

@Library('lib')_

helmchartPipeline{
  chart_path       = './kubernetes/charts/sparkapplication'
  chart_repository = 'helm-public'
  chart_version    = '1.5.0'  // Should from now on be alligned with CHANGELOG.md
}
